;; Set Linum-Mode on
(global-linum-mode t)
 
;; Linum-Mode and add space after the number
(setq linum-format "%d ")

(global-font-lock-mode t)

;; Remove menu bar
(menu-bar-mode 0)

;; Get rid of annyoing ~ files
(setq make-backup-files nil)

